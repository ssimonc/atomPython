import setuptools

setuptools.setup(name='atomPython',
      version='1.0',
      packages=setuptools.find_packages(),
      install_requires=['numpy', 'pandas', 'matplotlib', "scipy"]  # And any other dependencies needed
      

)

